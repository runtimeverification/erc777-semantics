test.preamble();

// Testing authorizeOperator(Operator) when Operator is a default operator.
// A token holder MAY authorize an already authorized operator. An AuthorizedOperator MUST be emitted each time.

test.isOperatorFor(8, 7);
test.authorizeOperator(8);
test.isOperatorFor(8, 7);
