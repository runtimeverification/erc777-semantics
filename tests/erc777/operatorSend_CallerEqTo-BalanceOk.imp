test.preamble();

// Testing operatorSend(From,To,Value,"","") when To = caller.
// All conditions are met, so it is expected to pass.

test.makeCaller(8);           // switch to 8
test.operatorSend(7, 8, 23, "", "");  // send 23 from 7 to 8
