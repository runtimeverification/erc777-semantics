test.preamble();

// Testing operatorSend(From,To,Value, "", "") when From = To.
// Send entire Balance

test.makeCaller(9);           // switch to 9
test.operatorSend(7, 7, 70, "", "");  // send entire balance
